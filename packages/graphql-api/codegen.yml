overwrite: true
schema: ./src/modules/${ACCOUNTS_MODULE}/schema.ts
require: ts-node/register/transpile-only
generates:
  ./src/modules/${ACCOUNTS_MODULE}/models.ts:
    plugins:
      - add: /* tslint:disable */
      - typescript
      - typescript-resolvers
